---
"webhands": minor
"@webhands/core": minor
---

First release: a CLI (and MCP server) that drives a real, persistent browser via Playwright, letting an agent or human control any website from a genuinely logged-in browser session.
